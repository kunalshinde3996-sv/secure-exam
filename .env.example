PORT=3000
JWT_SECRET=change-this-secret-in-production

# 32-byte AES-256-GCM key, as 64 hex characters. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PAPER_ENC_KEY=change-this-to-a-generated-64-char-hex-key
